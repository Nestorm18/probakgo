name: Release

on:
  workflow_call:
    inputs:
      tag:
        required: true
        type: string
      sha:
        required: true
        type: string
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag, for example vX.Y.Z
        required: true
        type: string
  push:
    tags: ['v*']

jobs:
  release:
    runs-on: ubuntu-latest
    environment: release
    permissions:
      contents: write
      id-token: write
      attestations: write
      artifact-metadata: write

    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          ref: ${{ inputs.sha || github.ref }}
          fetch-depth: 0

      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version: '1.26.6'
          cache: true

      - name: Resolve and validate release tag
        env:
          EVENT_NAME: ${{ github.event_name }}
          REQUESTED_TAG: ${{ inputs.tag }}
          REQUESTED_SHA: ${{ inputs.sha }}
          REF_NAME: ${{ github.ref_name }}
          EVENT_SHA: ${{ github.sha }}
        shell: bash
        run: |
          APP_VERSION=$(grep -oP 'var Version = "\K[^" ]+' internal/version/version.go)
          if [[ -n "$REQUESTED_TAG" && -n "$REQUESTED_SHA" ]]; then
            RELEASE_TAG="$REQUESTED_TAG"
            RELEASE_SHA="$REQUESTED_SHA"
          elif [[ "$EVENT_NAME" == "workflow_dispatch" ]]; then
            RELEASE_TAG="$REQUESTED_TAG"
            RELEASE_SHA="$EVENT_SHA"
          else
            RELEASE_TAG="$REF_NAME"
            RELEASE_SHA="$EVENT_SHA"
          fi
          if [[ ! "$RELEASE_TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Invalid release tag: $RELEASE_TAG"
            exit 1
          fi
          RELEASE_VERSION="${RELEASE_TAG#v}"
          if [[ "$APP_VERSION" != "$RELEASE_VERSION" ]]; then
            echo "Release tag $RELEASE_TAG does not match internal version: $APP_VERSION"
            exit 1
          fi
          CHECKOUT_SHA=$(git rev-parse HEAD)
          if [[ "$CHECKOUT_SHA" != "$RELEASE_SHA" ]]; then
            echo "Checked-out source does not match release commit $RELEASE_SHA"
            exit 1
          fi
          if TAG_SHA=$(git rev-parse --verify "refs/tags/$RELEASE_TAG^{commit}" 2>/dev/null); then
            if [[ "$TAG_SHA" != "$RELEASE_SHA" ]]; then
              echo "Existing tag $RELEASE_TAG points to $TAG_SHA, not $RELEASE_SHA"
              exit 1
            fi
          fi
          echo "RELEASE_TAG=$RELEASE_TAG" >> "$GITHUB_ENV"
          echo "RELEASE_SHA=$RELEASE_SHA" >> "$GITHUB_ENV"

      - name: Check whether release already exists
        id: existing_release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            echo "Release $RELEASE_TAG already exists; nothing to publish."
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Build server Linux amd64
        if: steps.existing_release.outputs.exists != 'true'
        run: |
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build \
            -ldflags="-s -w -X probakgo/internal/version.Version=${RELEASE_TAG#v}" \
            -o probakgo_linux_amd64 .

      - name: Build client Linux amd64
        if: steps.existing_release.outputs.exists != 'true'
        run: |
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build \
            -ldflags="-s -w -X probakgo/internal/version.Version=${RELEASE_TAG#v}" \
            -o probakgo-client_linux_amd64 ./client/

      - name: Build Windows client amd64
        if: steps.existing_release.outputs.exists != 'true'
        run: |
          CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go build \
            -ldflags="-s -w -X probakgo/internal/version.Version=${RELEASE_TAG#v}" \
            -o probakgo-windows-client_windows_amd64.exe ./client-windows/

      - name: Create checksums
        if: steps.existing_release.outputs.exists != 'true'
        run: sha256sum probakgo_linux_amd64 probakgo-client_linux_amd64 probakgo-windows-client_windows_amd64.exe > SHA256SUMS

      - name: Attest release binaries
        if: steps.existing_release.outputs.exists != 'true'
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-checksums: SHA256SUMS

      - name: Publish GitHub Release
        if: steps.existing_release.outputs.exists != 'true'
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          target_commitish: ${{ env.RELEASE_SHA }}
          generate_release_notes: true
          fail_on_unmatched_files: true
          files: |
            probakgo_linux_amd64
            probakgo-client_linux_amd64
            probakgo-windows-client_windows_amd64.exe
            SHA256SUMS
